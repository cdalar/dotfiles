# Copy this file to ~/.env.local and fill in your values.
# ~/.env.local is sourced by .bashrc but is NOT committed to git.

export HCLOUD_TOKEN=your_hetzner_cloud_token_here
export ONCTL_CLOUD=hetzner
